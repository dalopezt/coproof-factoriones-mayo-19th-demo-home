% Label: Upper Bound

**Theorem (Upper Bound).** For every natural number $n$ with $n \geq 10^7$, the digit factorial sum of $n$ is strictly less than $n$, i.e., $$\sigma(n) < n,$$ where $\sigma(n) = \sum_{d \in \text{digits}(n)} d!$

*Proof.* Unsolved.